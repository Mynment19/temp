# class 파일은 제외
*.class

index.txt

#윗 라인에 확장자가 a인 파일은 무시했지만 lib.a는 무시하지 않음
!lib.a

#현재 디렉토리에 있는TODO 파일은 무시하고 subdir/TODO처럼 하위 디렉토리에 있는 파일은 무시하지 않음
/TODO

#build 디렉토리에 있는 모든 파일은 무시
build/

#doc 폴더 아래에 모든 txt 파일 무시(doc/server/test.txt 파일은 무시하지 않음)
doc/*.txt

#doc 폴더 아래의 모든 .pdf 파일 무시(doc/server/1.pdf 무시)
doc/**/*.pdf

파일 무시하기 : .gitignore
  추적하지 않을 파일들에 대해서 기록
  처음에 하는 것이 좋음
